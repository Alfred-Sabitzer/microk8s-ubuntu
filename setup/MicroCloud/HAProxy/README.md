# Installation of a simple HA-Proxy

https://cloudinit.readthedocs.io/en/latest/reference/examples.html#yaml-examples

HAProxy is a hard to manage reverse proxy (especially when the backend is using https und has an URL in (eg. .../ui/:8443 ))