---
# busybox.yaml
# Deploys a BusyBox pod to rook-ceph Ceph RBD PVC provisioning and encryption.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rookrbd
  namespace: rook-ceph
  labels:
    app: rookrbd
    storage: rook
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ceph-rbd
  resources:
    requests:
      storage: 10G
---
# This is an example of a Kubernetes Deployment that uses a PersistentVolumeClaim on ceph shared storage.
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: rookrbd
    storage: rook
  name: rookrbd
  namespace: rook-ceph
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rookrbd
  template:
    metadata:
      labels:
        app: rookrbd
    spec:
      containers:
      #- image: busybox:latest
      - image: ubuntu:noble
        name: rookrbd
        command:
          - sh
          - -c
          - |
            sleep 10
            while true; do
              echo 'observability-rbd alive'
              echo $(date +"%Y%m%d %HH:%mm") [`hostname`] ' observability-rbd alive'  >> /mnt/rook/hello.txt
              sleep 30
            done
        volumeMounts:
          - name: rook
            mountPath: "/mnt/rook"
      volumes:
      - name: rook
        persistentVolumeClaim:
          claimName: rookrbd
---