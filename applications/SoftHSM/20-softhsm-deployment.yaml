---
# softhsm-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: softhsm
  namespace: ${NAMESPACE}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: softhsm
  template:
    metadata:
      labels:
        app: softhsm
    spec:
      containers:
      - name: softhsm
        image: alpine:latest
        volumeMounts:
          - name: softhsm-data
            mountPath: /var/lib/softhsm
          - name: softhsm-lib
            mountPath: /usr/lib/softhsm
        env:
          - name: SOFTHSM2_CONF
            value: "/etc/softhsm2.conf"
        command: ['sh', '-c', 'echo The app is running! && sleep infinity']
      initContainers:
      - name: softhsminit
        image: alpine:latest
        volumeMounts:
          - name: softhsm-data
            mountPath: /var/lib/softhsm
          - name: softhsm-lib
            mountPath: /usr/lib/softhsm
          - name: softhsm-config
            mountPath: /app/softhsm.sh
            subPath: softhsm.sh
        env:
          - name: SOFTHSM2_CONF
            value: "/etc/softhsm2.conf"
        command:
          - "sh"
          - "-c"
          - "/app/softhsm.sh"
        #ports:
        #- containerPort: 8080
        # Note: The container does not need open ports unless you plan for remote mgmt
      volumes:
      - name: softhsm-data
        persistentVolumeClaim:
          claimName: softhsm-pvc
      - name: softhsm-lib
        emptyDir:  {}
      - name: softhsm-config
        configMap:
          name: softhsm
---