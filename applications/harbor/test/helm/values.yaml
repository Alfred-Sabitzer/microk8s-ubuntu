namespace: "test-environment"  # Der Ziel-Namespace im Cluster

replicaCount: 1

image:
  registry: "harbor.test.slainte.at"
  project: "test"
  repository: "dummy"
  tag: "20260901"
  digest: "sha256:63c49e1cdae675cf9f93bd95db9625938e49811aec1e28f4b268dd3ab72bc1af"
  pullPolicy: IfNotPresent

# Neu: Logindaten für das automatische Erzeugen des Secrets
registryCredentials:
  username: "alfred"
  password: "PASSWORD_HERE"

buildInfo:
  tool: "podman"

service:
  type: ClusterIP
  port: 80

  # In Ihrer values.yaml ändern/ergänzen:
service:
  type: ClusterIP
  port: 8080  # Der Port, auf dem die Anwendung im Container lauscht
